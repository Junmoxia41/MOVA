package com.mova.santaclara

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Test unitario local de ejemplo (host). Se sustituirá por tests reales
 * de casos de uso, validaciones y sincronización en fases posteriores.
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
